<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<svg
    xmlns="http://www.w3.org/2000/svg"
    version="1.1"
    width="1000px"
    height="1000px">
    {? use 0 as x:
        {? use 0 as y:
            {? for class in classes:
                <g transform="translate({{ x }}, {{ y }})">
                    {% view class %}
                </g>
                {% set x {{ x + class.width + 20 }} %}
            ?}
        ?}
    ?}
</svg>
